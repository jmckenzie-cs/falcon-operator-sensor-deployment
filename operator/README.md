# Placeholder for pinned operator release manifest.
# Download the appropriate version from:
#   https://github.com/crowdstrike/falcon-operator/releases
#
# Example:
#   curl -LO https://github.com/crowdstrike/falcon-operator/releases/download/v1.7.0/falcon-operator.yaml
#
# Pin to a specific version in your environment by committing the downloaded
# manifest here and updating OPERATOR_VERSION in scripts/install.sh.
#
# The install.sh script downloads this manifest directly from GitHub at
# the version specified by OPERATOR_VERSION. To use a locally committed
# manifest instead, update the install.sh step accordingly.
